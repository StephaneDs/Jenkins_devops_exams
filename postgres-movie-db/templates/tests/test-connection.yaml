apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgres-movie-db.fullname" . }}-test-connection"
  labels:
    {{- include "postgres-movie-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postgres-movie-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
